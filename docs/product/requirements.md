# Product Requirements Matrix

Status: Product source of truth with current MVP status labels.

Source: `PLAN.md`. Current stable Go was verified externally as `go1.26.4` on
2026-07-01; recent agent-loop validation also used `go1.26.4 linux/amd64`.

## Current Behavior Status

| Area | Status | Notes |
| --- | --- | --- |
| Mock chat | Ready | Runs without Twitch credentials or network access. |
| Multi-channel live IRC read/send | Partial | Reads, sends, replies, and sends `/me` actions for configured channels with environment, flat config, or saved credentials on supported Unix platforms. Broader manual Twitch evidence remains future work. |
| Diagnostics | Ready | `twi doctor` reports config path, credential presence, Twitch OAuth identity/expiry/scope validation, refresh availability, username mismatch, Twitch IRC reachability, terminal hints, cache writability/pruning, and feature modes. Live chat also preflights token validation before IRC startup when validation is reachable. |
| Login/setup | Partial | `twi setup` writes non-secret config values and, on supported Unix builds, can hand off to `twi login`. On supported Unix builds, `twi login` runs the browser/local-callback OAuth flow or a `--dry-run` explanation, validates returned tokens, and saves them without printing them through the restrictive credential-file fallback. Non-Unix builds do not perform saved-credential browser login; live chat uses env/config credentials there. |
| Multi-channel UX | Partial | Per-channel state, local view filters, live routing, keyboard-first sidebar, command palette, focus-aware system-event notifications, optional mouse controls, selected-message inspect, and active live IRC reconnect restart are implemented; real two-channel Twitch manual evidence and transport-specific per-channel reconnect isolation remain pending. |
| Avatars, badges, emotes, and emoji | Ready | There is no terminal-image rendering path. Avatars always render as an `[XY]` initials chip (or nothing when `avatar_mode = "off"`), badges as compact text labels, Twitch emotes as their matched text token, and standard emoji as native Unicode glyphs. |

## MVP Scope

MVP means the smallest product that proves the chat client can run as a real TUI:

- Twitch chat read and send over IRC for configured channels.
- Credentials from environment variables, config file, or saved credentials on supported Unix platforms, with CLI overrides for channel and config path.
- Bubble Tea shell with status bar, chat viewport, composer, compact help, resize handling, and text fallbacks.
- Typed-in reveal for incoming mock and live messages.
- Test fakes for chat transport, send results, config, and animation timing.
- No terminal image rendering: avatars, badges, emotes, and emoji always render as text and require no terminal graphics capability.

Terminal-image rendering (Kitty graphics) was implemented and later removed as a deliberate product decision (see [adr/0003-use-kitty-graphics-as-first-image-protocol.md](../adr/0003-use-kitty-graphics-as-first-image-protocol.md), now superseded); it is not planned scope going forward. Later scope includes activity charts and additional transport protocols.

## Support Tiers

| Tier | Target | Required behavior |
| --- | --- | --- |
| Tier 1 | Modern true-color Unicode terminals | Full color TUI, mouse where available, text-only avatars/badges/emotes/emoji (initials chip, badge labels, emote tokens, Unicode glyphs), no layout reflow. |
| Tier 2 | Basic terminals with reduced visual capability | Readable chat, reduced styling, no required mouse, animations can reduce or disable automatically. |

## Matrix

| ID | Area | Requirement | MVP | Later | Testability |
| --- | --- | --- | --- | --- | --- |
| R-001 | Chat read | Connect to Twitch IRC over TLS, authenticate, join configured channels, request tags/commands, and render live `PRIVMSG`, notices, `USERNOTICE` events, room state, moderation, reconnect, and connection events through normalized messages. | Configured-channel IRC join, core message/notice/status events, normalized `USERNOTICE` system-event IDs, fake client tests. Twitch IRC connect/reconnect/disconnect callbacks are connection-level, not independent per-channel events. | EventSub-compatible transport, richer event coverage. | Fake `ChatClient` integration tests; manual low-traffic channel check; reconnect state tests. |
| R-002 | Chat send | Send composer text to the active channel using IRC `Say`; keep send status visible and preserve user text on failure. | Normal sends, selected-message replies, `/me` actions, and failed-send feedback. | Optimistic echo reconciliation and richer rate-limit guidance. | Fake sender success/failure tests; manual send to test channel. |
| R-003 | Avatars | Reserve an avatar column and show a stable, always-text author indicator. | `[XY]` initials chip derived from the author's display name, or hidden entirely when `avatar_mode = "off"`. No image lookup, download, or rendering. | None; image-backed avatars were implemented and then removed. | Golden rows with fallback; `avatar_mode` unit tests. |
| R-004 | Twitch emotes | Preserve Twitch emote tokens from IRC tags and render readable fallback tokens. | Parse or preserve emote metadata enough to avoid corrupt text fallback; emote text tokens always render as-is. | None; image-backed emotes were implemented and then removed. | Unit tests for emote position parsing and fallback rows. |
| R-005 | Emoji | Preserve Unicode emoji and grapheme clusters in messages. | Native Unicode emoji fallback with grapheme-safe reveal/wrap. | None; image-backed emoji were implemented and then removed. | Grapheme tests; golden rows with emoji. |
| R-006 | Typed-in animation | New visible messages reveal quickly without blocking input, scrolling, sending, network receive, or reconnect handling. | Tick-driven reveal over normalized/render fragments for mock and live text fallback rows; modes `off`, `reduced`, `fast`. | Expressive mode, newest-row pulse, high-volume tuning. | Fake clock frame tests; high-throughput degradation tests; manual viewport check. |
| R-007 | Auth | Accept Twitch username and OAuth token securely and require `chat:read`/`chat:edit` for IRC MVP. | Env/config credentials plus saved credentials on Unix; actionable missing/invalid credential errors; doctor token validation; live startup token validation with scope checks when validation is reachable; no secret printing; `twi login` OAuth validation; restrictive Unix credential-file persistence; setup login handoff; refreshed-token persistence after IRC auth refresh where the credential store is supported; credential storage boundary and fakes. | Auth flags only if needed; non-Unix saved credentials remain unsupported. | Config/auth/storage validation tests; redaction tests; manual invalid-token check. |
| R-008 | Config | Resolve effective config from CLI overrides > environment > config file > saved credentials > defaults. | Env/config support for username, token, channels, avatar mode, and animation mode; CLI overrides for config path and channels; login reads client ID/secret from env/config; setup writes non-secret config values; saved credential values fill empty credential fields on supported Unix platforms. | Auth/mode flags, explicit config-to-credential migration UX. | Precedence tests; `config show` redaction tests; setup wizard tests; path tests by platform. |
| R-009 | Multi-channel | Preserve channel-specific history, composer state, connection state, unread counts, local filters, and system-event attention cues. | Join multiple configured channels; route messages, unread counts, drafts, replies, active sends, local view filters, scroll state, and inactive-channel system-event notifications by channel. Local filters can narrow the active view to mentions, broadcaster/mod/VIP messages, notices/system rows, or error-like rows without deleting channel history. Normal/wide terminals show a keyboard-first sidebar, while narrow terminals collapse channel state into the status line. Connection-level IRC states are copied onto configured channel states; manual reconnect restarts the active live IRC transport without clearing per-channel UI state. | Mouse switching polish and isolated per-channel reconnect semantics if a future transport exposes them. | Model tests with two fake channels; manual two-channel Twitch check. |
| R-010 | Mouse | Enable optional mouse interactions without making core workflows mouse-dependent. | `enable_mouse` defaults on and can be disabled through config/env. When enabled, mouse wheel scrolls the chat viewport, sidebar clicks switch channels, composer clicks focus input, and message clicks select reply context. Keyboard equivalents remain available. | Clickable URLs and richer hit regions after security/UX review. | Bubble Tea event tests; manual terminal mouse check. |
| R-011 | Diagnostics | Surface connection, send, auth, terminal capability, system-event notification, and degraded-mode status without destroying chat context. | Status bar/errors in TUI plus focus-aware desktop notifications for normalized system events such as raids, with terminal-bell fallback when the host notifier is unavailable; `twi doctor` checks for config path, credential presence, Twitch OAuth identity/expiry/scope validation, refresh availability, username mismatch, Twitch reachability, terminal color/mouse signals, cache writability/pruning, feature modes, and secret redaction. Live chat startup validates definitive token failures before IRC connects when validation is reachable. Selected-message inspect shows redacted normalized and raw tag diagnostics without replacing composer, reply, or scroll state. Opt-in JSON debug logs cover auth, transport, send, asset, render, and connection diagnostics with curated redacted fields. | Richer in-app auth diagnostics. | Unit tests for diagnostic output redaction, startup validation, and debug log redaction; manual `doctor` checks. |
| R-012 | Fallbacks | Every optional feature degrades or renders visibly and intentionally: avatars/badges/emotes/emoji always as text (initials/labels/tokens/Unicode), animation to reduced/off. | Text-only avatar/badge/emote/emoji rendering, reduced/off animation, no blocking asset paths. | Cache-aware degraded states for future async features. | Golden fallback snapshots; fake failure tests. |
| R-013 | Layout and UX | Use a colorful, high-contrast TUI with status bar, viewport, composer, help, and responsive narrow/wide layout. | Root Bubble Tea shell with resize-safe layout; a darker theme-derived canvas behind raised pane surfaces; icon-bearing pane titles, quiet frames, role-colored left rails, seamless mirrored shared-clock focus gradients on auxiliary panes, and a deliberately static Chat frame/title; staged animated block-logo splash; author-grouped message surfaces and rails with a non-selectable horizontal separator only when the visible author changes, plus mail glyphs, emoji-rich text, and stable distinct contrast-safe username colors; an OpenCode-inspired borderless composer surface with a focus rail, block cursor, reply context, and channel/send-state footer; a top bar showing the configured Twitch username and active chat; keyboard-first sidebar; command palette; local filter controls; optional mouse controls; and selected-message inspect without raw debug-console output. | Activity charts and richer sidebar polish. | Snapshot/golden layout tests at narrow/normal/wide widths; deterministic splash/gradient/pane/message-group/composer tests; manual resize test. |
| R-014 | Architecture | Keep UI, Twitch transport, rendering, assets, animation, config, and storage behind testable boundaries. | `ChatClient`, normalized message model, config interfaces, animation clock fakes. | EventSub adapter. | Compile-time interface checks; unit tests per package; no UI dependency on Twitch library types. |
| R-015 | Security | Never print secrets in logs, errors, snapshots, config output, or tests. | Redaction helpers and tests before config/auth output ships; structured debug logging is opt-in and redacted by default; credential storage DTO keeps default encoding redacted and requires an explicit storage-owned reveal path; restrictive Unix credential-file implementation rejects unsafe modes and symlinks and uses no-follow opens; non-Unix builds do not save credentials. | Additional storage backends are out of scope unless product direction changes. | Secret fixture tests; search logs/snapshots for token patterns before handoff. |
| R-016 | Tooling | Use official Go modules and the current stable Go toolchain. | `go 1.26`, `toolchain go1.26.4`, module tools via Go tool directives once module exists. | Periodic latest stable verification before implementation/release. | `go version`, `go mod tidy`, `go fmt ./...`, `go test ./...`, `go vet ./...`, later `govulncheck`. |

## Phase 0 Exit Check

- Requirements matrix exists: this document.
- MVP and later scope are explicit.
- Target terminal support tiers are explicit.
- Current stable Go version is recorded from the provided verification context.
- ADRs exist for IRC transport, Helix asset wrapper, Kitty graphics, normalized message model, animation scheduler, and Go toolchain management.
