// Package emoji provides small Unicode helpers used by render and asset code.
//
// The package keeps emoji detection logic separate from chat rendering so text
// fallbacks and image-asset lookup can share the same tokenization behavior.
package emoji
